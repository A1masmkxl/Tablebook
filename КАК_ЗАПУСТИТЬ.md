# 🚀 Как запустить TableBook

## Способ 1: Автоматический (самый простой)

### Запуск всего сразу:
Дважды кликните на файл:
```
START_ALL.bat
```

Этот скрипт автоматически запустит:
- ✅ Бэкенд на порту 8081
- ✅ Фронтенд на порту 3000

---

## Способ 2: Ручной запуск

### Шаг 1: Подготовка базы данных (только первый раз)

1. Откройте **pgAdmin 4**
2. Подключитесь к серверу PostgreSQL
3. Откройте базу данных `tablebook` (или создайте, если её нет)
4. Откройте **Query Tool** (Alt+Shift+Q или правая кнопка → Tools → Query Tool)
5. Откройте файл `java-backend/database_schema.sql`
6. Скопируйте **весь** текст из файла
7. Вставьте в Query Tool
8. Нажмите **Execute** (▶) или **F5**

### Шаг 2: Запуск бэкенда

**Вариант А: Через файл**
```
Дважды кликните: java-backend\START_SIMPLE.bat
```

**Вариант Б: Через командную строку**
```bash
cd java-backend
mvn spring-boot:run
```

**Должно появиться:**
```
Started TableBookApplication in X.XXX seconds
```

**Бэкенд будет доступен на:** http://localhost:8081

### Шаг 3: Запуск фронтенда (в НОВОМ терминале)

**Вариант А: Через командную строку**
```bash
cd my-react-app
npm start
```

**Вариант Б: Через PowerShell**
```powershell
cd my-react-app
npm start
```

**Должно появиться:**
```
Compiled successfully!
```

**Фронтенд будет доступен на:** http://localhost:3000

---

## 📋 Порядок запуска:

1. **Сначала** → Бэкенд (порт 8081)
2. **Потом** → Фронтенд (порт 3000)

**Важно:** Бэкенд должен быть запущен ДО запуска фронтенда!

---

## ✅ Проверка работы:

### Проверка бэкенда:
Откройте в браузере: http://localhost:8081/api/restaurants

**Должен вернуться JSON** с ресторанами.

### Проверка фронтенда:
Откройте в браузере: http://localhost:3000

**Должен открыться интерфейс** приложения.

---

## 🔧 Если что-то не работает:

### Ошибка: "Connection refused" или "ERR_CONNECTION_REFUSED"

**Причина:** Бэкенд не запущен

**Решение:**
1. Проверьте окно, где запущен бэкенд
2. Убедитесь, что видите `Started TableBookApplication`
3. Если нет - запустите бэкенд заново

### Ошибка подключения к базе данных

**В логах бэкенда:**
- `Connection refused` → PostgreSQL не запущен
- `password authentication failed` → Неправильный пароль
- `database "tablebook" does not exist` → База данных не создана

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте пароль в `java-backend/src/main/resources/application.properties` (строка 8)
3. Выполните SQL скрипт `database_schema.sql` в pgAdmin

### Порт занят

**Ошибка:** `Port 8081 already in use`

**Решение:**
1. Остановите процесс, использующий порт
2. Или измените порт в `application.properties`:
   ```properties
   server.port=8082
   ```
3. Обновите URL во фронтенде в `my-react-app/src/utils/constants.js`

---

## 🎯 Быстрая проверка:

```bash
# Проверка портов
netstat -ano | findstr ":8081"  # Бэкенд
netstat -ano | findstr ":3000"  # Фронтенд

# Тест API
curl http://localhost:8081/api/restaurants
```

---

## 📝 Тестовые пользователи:

После первого запуска создаются автоматически:
- **Админ:** `admin@tablebook.kz` / `admin123`
- **Пользователь:** `user@test.kz` / `user123`

---

## 💡 Рекомендации:

1. **Используйте `START_ALL.bat`** - это самый простой способ
2. **Проверяйте окна PowerShell** - там видны все ошибки
3. **Сначала база данных** - выполните SQL скрипт перед первым запуском
4. **Сначала бэкенд** - запускайте бэкенд перед фронтендом

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в окнах PowerShell
2. Откройте файл `QUICK_FIX.md` или `TROUBLESHOOTING.md`
3. Убедитесь, что PostgreSQL запущен и база данных создана

