# 🚀 Запуск бэкенда - Пошаговая инструкция

## ❌ Проблема: ERR_CONNECTION_REFUSED

Это означает, что бэкенд не запущен или не может подключиться к базе данных.

## ✅ Решение:

### Шаг 1: Проверьте PostgreSQL

1. Убедитесь, что PostgreSQL **запущен**
2. Проверьте в pgAdmin, что сервер доступен

### Шаг 2: Проверьте базу данных

1. Откройте pgAdmin
2. Проверьте, что база данных `tablebook` **существует**
3. Если нет - создайте:
   ```sql
   CREATE DATABASE tablebook;
   ```

### Шаг 3: Выполните SQL скрипт

1. В pgAdmin: база данных `tablebook` → **Query Tool**
2. Откройте файл `java-backend/database_schema.sql`
3. Скопируйте **весь** текст
4. Вставьте в Query Tool
5. Нажмите **Execute** (F5)

### Шаг 4: Проверьте пароль

Откройте файл: `java-backend/src/main/resources/application.properties`

Строка 8 должна содержать правильный пароль PostgreSQL:
```properties
spring.datasource.password=postgres
```

Замените `postgres` на ваш реальный пароль PostgreSQL.

### Шаг 5: Запустите бэкенд

**Вариант А: Через батник**
```
Дважды кликните: FIX_BACKEND.bat
```

**Вариант Б: Через командную строку**
```bash
cd java-backend
mvn spring-boot:run
```

### Шаг 6: Проверьте логи

В окне, где запущен бэкенд, должны быть сообщения:

✅ **Успех:**
```
Started TableBookApplication in X.XXX seconds
```

❌ **Ошибка подключения к БД:**
```
Connection refused
password authentication failed
```

Если видите ошибку - вернитесь к шагам 1-4.

### Шаг 7: Проверка работы

После запуска откройте в браузере:
http://localhost:8081/api/restaurants

**Должен вернуться JSON** с ресторанами.

---

## 🔍 Быстрая диагностика:

### Проверка порта:
```bash
netstat -ano | findstr :8081
```

Если порт не слушается - бэкенд не запущен.

### Проверка PostgreSQL:
```bash
netstat -ano | findstr :5432
```

Если порт не слушается - PostgreSQL не запущен.

---

## ⚠️ Частые ошибки:

### 1. "Connection refused"
**Причина:** PostgreSQL не запущен  
**Решение:** Запустите PostgreSQL

### 2. "password authentication failed"
**Причина:** Неправильный пароль  
**Решение:** Проверьте пароль в `application.properties`

### 3. "database tablebook does not exist"
**Причина:** База данных не создана  
**Решение:** Создайте базу данных в pgAdmin

### 4. "Table 'users' doesn't exist"
**Причина:** SQL скрипт не выполнен  
**Решение:** Выполните `database_schema.sql` в pgAdmin

---

## 📝 Чек-лист перед запуском:

- [ ] PostgreSQL запущен
- [ ] База данных `tablebook` создана
- [ ] SQL скрипт `database_schema.sql` выполнен
- [ ] Пароль правильный в `application.properties`
- [ ] Бэкенд запущен (видно `Started TableBookApplication`)
- [ ] Порт 8081 слушается (проверка через `netstat`)


