# ✅ Исправлена ошибка "Ресторан не найден"

## Проблема:
При нажатии кнопки "Забронировать" появлялась ошибка "Ресторан не найден".

## Причина:
Фронтенд использовал старое поле `_id` (из MongoDB), а после миграции на PostgreSQL используется поле `id` (Long).

## Исправления:

### 1. BookingModal.jsx
- ❌ Было: `restaurant: restaurant._id`
- ✅ Стало: `restaurantId: restaurant.id`

### 2. RestaurantCard.jsx
- ❌ Было: `/restaurant/${restaurant._id}`
- ✅ Стало: `/restaurant/${restaurant.id}`

### 3. MyBookings.jsx
- ❌ Было: `key={booking._id}`, `booking.restaurant._id`, `handleCancelBooking(booking._id)`
- ✅ Стало: `key={booking.id}`, `booking.restaurant.id`, `handleCancelBooking(booking.id)`

### 4. Home.jsx
- ❌ Было: `key={restaurant._id}`
- ✅ Стало: `key={restaurant.id}`

### 5. Restaurants.jsx
- ❌ Было: `key={restaurant._id}`
- ✅ Стало: `key={restaurant.id}`

### 6. Admin/ManageRestaurants.jsx
- ❌ Было: `editingRestaurant._id`, `key={restaurant._id}`, `handleDelete(restaurant._id)`
- ✅ Стало: `editingRestaurant.id`, `key={restaurant.id}`, `handleDelete(restaurant.id)`

### 7. Admin/ManageUsers.jsx
- ❌ Было: `key={user._id}`, `handleRoleChange(user._id, ...)`
- ✅ Стало: `key={user.id}`, `handleRoleChange(user.id, ...)`

### 8. Admin/ManageBookings.jsx
- ❌ Было: `key={booking._id}`, `#{booking._id.slice(-6)}`, `handleStatusChange(booking._id, ...)`
- ✅ Стало: `key={booking.id}`, `#{booking.id.toString().slice(-6)}`, `handleStatusChange(booking.id, ...)`

### 9. Admin/Dashboard.jsx
- ❌ Было: `key={booking._id}`
- ✅ Стало: `key={booking.id}`

---

## ✅ Теперь бронирование работает!

Попробуйте снова нажать "Забронировать" - ошибка должна исчезнуть.

---

## 📝 Примечание:

Все изменения связаны с миграцией с MongoDB (где ID был строкой `_id`) на PostgreSQL (где ID - число `id` типа Long).

