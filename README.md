# TableBook - Система бронирования столиков в ресторанах

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

Просто запустите файл:
```bash
START_ALL.bat
```

Этот скрипт автоматически:
1. Запустит бэкенд на порту 8081
2. Подождет 30 секунд
3. Запустит фронтенд на порту 3000

### Вариант 2: Ручной запуск

#### 1. Настройка базы данных

1. Откройте pgAdmin 4
2. Подключитесь к PostgreSQL
3. Откройте базу данных `tablebook`
4. Откройте Query Tool (Alt+Shift+Q)
5. Скопируйте и выполните содержимое файла `java-backend/database_schema.sql`

#### 2. Запуск бэкенда

```bash
cd java-backend
mvn spring-boot:run
```

Или используйте:
```bash
java-backend\START.bat
```

Бэкенд будет доступен на: http://localhost:8081

#### 3. Запуск фронтенда

```bash
cd my-react-app
npm start
```

Фронтенд будет доступен на: http://localhost:3000

## 📋 Требования

- Java 17+
- Maven 3.6+
- PostgreSQL 12+
- Node.js 14+
- npm или yarn

## 🔧 Конфигурация

### База данных

Настройки в `java-backend/src/main/resources/application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/tablebook
spring.datasource.username=postgres
spring.datasource.password=postgres
```

### API URL

Настройки в `my-react-app/src/utils/constants.js`:
```javascript
export const API_URL = 'http://localhost:8081/api';
```

## ✅ Проверка работы

После запуска проверьте:
- Бэкенд: http://localhost:8081/api/restaurants
- Фронтенд: http://localhost:3000

Или используйте:
```bash
CHECK_STATUS.bat
```

## 📚 Документация

- `java-backend/PGADMIN_GUIDE.md` - Полный гайд по работе с pgAdmin 4
- `java-backend/SETUP_DATABASE.md` - Инструкция по настройке базы данных
- `java-backend/README.md` - Документация бэкенда

## 🎯 Тестовые пользователи

После первого запуска создаются:
- **Админ:** `admin@tablebook.kz` / `admin123`
- **Пользователь:** `user@test.kz` / `user123`

## 🛠️ Решение проблем

### Бэкенд не запускается
1. Проверьте, что PostgreSQL запущен
2. Проверьте, что база данных `tablebook` существует
3. Проверьте пароль в `application.properties`

### Фронтенд не подключается к API
1. Убедитесь, что бэкенд запущен
2. Проверьте URL в `constants.js`
3. Проверьте CORS настройки

### База данных пустая
Выполните SQL скрипт `database_schema.sql` в pgAdmin


